# Caddy MCP

Caddy Reverse Proxy MCP Server for Agentic AI!

## Environment Variables
- `CADDY_URL`: The base service URL.
- `CADDY_TOKEN`: The API / bearer token for authentication.

## CLI Usage
```bash
python -m caddy_mcp --help
```
