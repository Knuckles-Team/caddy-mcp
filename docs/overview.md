# Technical Overview — Caddy MCP

This package provides native integration with Caddy MCP services for Pydantic AI agents.

## Architecture
Integrates a high-performance REST client with FastMCP to expose clean tool tags under the `CADDY` prefix.
